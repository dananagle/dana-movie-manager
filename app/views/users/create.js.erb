<% if @user.save %>
<% session[:user_id] == @user.id %>
  $("#interest-modal").modal("hide");
  window.location.reload();

<% else %>
  $(".modal-body").html("<%= j render partial: 'signupform.html.erb'%> ");
  $("#interest-modal").modal("show");
<% end %>
